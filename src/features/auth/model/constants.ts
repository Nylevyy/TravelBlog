const NAMESPACE = '@@saga-auth';

export const AUTHORIZE_ACTION = `${NAMESPACE}/authorize`;

export const LOG_IN_ACTION = `${NAMESPACE}/login`;

export const LOG_OUT_ACTION = `${NAMESPACE}/logout`;

export const REGISTER_ACTION = `${NAMESPACE}/register`;
